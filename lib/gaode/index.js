import normalMap from './normal.map';
import satelliteMap from './satellite.map';
import satelliteAnnotion from './satellite.annotion';
export default {
  normalMap,
  satelliteMap,
  satelliteAnnotion
};
